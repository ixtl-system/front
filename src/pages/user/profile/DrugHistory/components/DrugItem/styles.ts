import styled from "styled-components";

export const DrugContainer = styled.div`
  margin: 20px 0 0;

  div > p {
    margin: 10px 0 5px;
  }

  div > label {
    display: flex;
    align-items: center;
    cursor: pointer;
    width: max-content;
    gap: 5px;
  }
`;